# Core training dependencies
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.27.0
bitsandbytes>=0.43.0   # needed for 4-bit QLoRA quantization

# For evaluation
rouge-score>=0.1.2     # ROUGE for open-ended text fields

# General utility
tqdm>=4.66.0
numpy>=1.24.0
anthropic>=0.25.0       # Claude Sonnet API for the frontier baseline
